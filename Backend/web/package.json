{
  "name": "pinkauth-web",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "npm install && wrangler deploy",
    "preview": "npm install && wrangler dev"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
